version: "3.2"
services:
  php:
    build: './php/'
    networks:
      - backend
    volumes:
      - ./blog/:/var/www/html/
  apache:
    build: './apache/'
    depends_on:
      - php
      - db
    networks:
      - frontend
      - backend
    ports:
      - "8080:80"
    volumes:
      - ./blog/:/var/www/html/
  db:
    build: './mysql/'
    restart: always
    networks:
      - backend
    volumes:
      -./blog/blog1.sql:/docker-entrypoint-initdb.d
    environment:
      - MYSQL_ROOT_PASSWORD=toor
      - MYSQL_DATABASE=blog
      - MYSQL_USER=MYSQL_USER
      - MYSQL_PASSWORD=toor
networks:
  frontend:
  backend: